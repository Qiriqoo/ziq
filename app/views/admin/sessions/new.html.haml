#admin-connect
  %h2 Administration
  = simple_form_for User.new, as: :user, url: admin_path do |f|
    = f.input :email, required: false, autofocus: true
    = f.input :password, required: false
    .form-actions
      = f.button :submit, "Se connecter"